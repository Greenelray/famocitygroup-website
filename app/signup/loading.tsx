import { PageLoading } from "@/components/page-loading";

export default function Loading() {
  return (
    <PageLoading
      eyebrow="Create Account"
      title="Opening your student profile."
      message="Preparing the signup flow so your Famocity course access is linked to the right learner account."
    />
  );
}
