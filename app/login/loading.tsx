import { PageLoading } from "@/components/page-loading";

export default function Loading() {
  return (
    <PageLoading
      eyebrow="Student Login"
      title="Opening secure sign in."
      message="Preparing the Famocity login experience and checking the fastest path into your dashboard."
    />
  );
}
